/*
1. hamne padha tha ki hm integer k array bana skte h, jaise hame students ke roll no.store krne h, to 100 student ke liye hamne 
int studentroll[100];
float price[100];
to yaha pr phle ham datatype(int,float) likhte h,fir variable(studentroll,price) fir quantity([100]) oaise hi,hm structure k v array bana skte h..

2. ARRAY OF STRUCTURE :- phle data type(structstsudent) ,fir variable name(ECE), fir quantity([100])

- struct student ECE[100];   // y 3 alg alg array ho gye
- struct student COE[100];
- struct student IT[100];











*/