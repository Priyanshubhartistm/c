/*
1. jis trh se ham alg alg data types ke variables ko pass kr skte h, use trh se ham structur ke variables ko v pass ker skte h.."ek function" ke pass.

2. Passing structure to function  :-


 //Function Prototype
void printInfo(struct student s1)         = 

i) normaly fun. k type likhte h(void printInfo) ,

ii) iske bad agar parameters structure aana h,to sbse phle type likhenge(struct student s1) to data type ho jayega:-struct student or variable :- s1.








*/