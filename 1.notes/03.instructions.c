// Instructions:- these are statements in a program.

/* 3types:-
1.)Type declaration instructions:-declare var before using it.

VALID:-
int a=22;
int b=a;
int c=b*2;
int d=1,e;

MULTIPLE VARIABLE KO EK SATH DECLARE KR SAKTE H..HM....
int a,b,c;
a=b=c=1;

2.) Arithmetic instructions:- a+b (where 'a,b' are operand; and '+' are operator{+,-,*,/,%}).

NOTE:-single variable on the LHS.
RHS RULE:- VAR= a+b-c*d.

a) pow(x,y)for x to the power y. example:- a=pow(b,c);  but upr ek header file add karna hoga.(#include<math.h>)
b) modular operator:-a % b,returns reminder for int
3%2=1
-3%2=-1

VALID:-
a=b+c
a=b*c
a=b/c

ii) type conversion :- jb v different operands hote h.. unke agr same type hh..to output v same type k ayega lkin uske data type alg alg type k h..to output bhi alg data type k ayega.

int output int       = int         
int output float     = float     // ek integer value h..or dusara float value to output ayega float.   
float output float   = float        



3. )Control instructions:-
used to determine flow of program
i) sequence control    = sare process control me chalta h..(ex:- int main ke bd (){
})
ii) decision control   = if,else
iii) loop control      = for loop,while loop,different loop padhenge yani kise kam ko hm br br karenge to loop padhenge.
iv) case control         = 
jaise monday to sat kam pe jana h..sunday ko ghr pe rahkr rest lena h..ise pure ek follow ko hm code me kaise dikha skte h..variable ki help se or cases ki help se ki kn se din me ghr pr rhna h..kn se din me km pr jana h.. 
to use statement ka jab bhi hm likh rahe hote h..un instructions ko likh rahe hote h..to unko hm case instructions ke andr rakhte h..
*/